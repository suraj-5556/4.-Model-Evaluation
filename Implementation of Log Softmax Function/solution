import numpy as np

def log_softmax(scores: list) -> np.ndarray:
	# Your code here
	count=0
    for i in scores:
        count=count+np.e**(i-max(scores))
    result=[]
    for i in scores:
        result.append(i-max(scores)-np.log(count))
    return result
