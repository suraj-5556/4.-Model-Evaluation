import numpy as np
def recall(y_true, y_pred):
    x=y=0
    for i in range (0,len(y_true)):
        if y_true[i]==1:
            x=x+1
            if y_pred[i]==1:
                y=y+1
    return round(y/x,3)
    
